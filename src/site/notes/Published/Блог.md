---
{"dg-publish":true,"permalink":"/published/blog/","title":"Блог | Мои статьи и туториалы"}
---


# 📝 Блог

Здесь я делюсь знаниями и опытом через статьи, туториалы и размышления.

---
dg-publish: true
title: Блог | Мои статьи и туториалы
---

# 📝 Блог

Здесь я делюсь знаниями и опытом через статьи, туториалы и размышления.

<style>
  * {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  }

  .blog-filters {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin: 20px 0 30px 0;
    align-items: center;
  }

  .filter-label {
    font-weight: 600;
    color: #333;
    margin-right: 10px;
  }

  .filter-btn {
    padding: 6px 14px;
    border: 1px solid #ddd;
    background: white;
    color: #333;
    border-radius: 20px;
    cursor: pointer;
    font-size: 13px;
    transition: all 0.2s ease;
  }

  .filter-btn:hover {
    background: #f5f5f5;
  }

  .filter-btn.active {
    background: #667eea;
    color: white;
    border-color: #667eea;
  }

  .blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 25px;
    margin: 30px 0;
  }

  .blog-card {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 12px;
    overflow: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    text-decoration: none;
    color: #333;
    display: flex;
    flex-direction: column;
    cursor: pointer;
  }

  .blog-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .blog-card-image {
    width: 100%;
    height: 200px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 48px;
  }

  .blog-card-content {
    padding: 20px;
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .blog-card-title {
    font-size: 18px;
    font-weight: 600;
    margin: 0 0 10px 0;
    color: #1a1a1a;
  }

  .blog-card-excerpt {
    font-size: 14px;
    color: #555;
    margin: 0 0 15px 0;
    line-height: 1.5;
    flex: 1;
  }

  .blog-card-meta {
    display: flex;
    justify-content: space-between;
    font-size: 12px;
    color: #888;
  }

  .blog-card-tag {
    display: inline-block;
    background: #f0f0f0;
    padding: 4px 8px;
    border-radius: 4px;
    color: #667eea;
    font-weight: 500;
  }

  .loading {
    text-align: center;
    padding: 40px;
    color: #666;
  }

  .no-posts {
    grid-column: 1/-1;
    text-align: center;
    color: #999;
    padding: 40px;
  }
</style>

<div id="blog-filters" class="blog-filters">
  <span class="filter-label">Фильтр:</span>
</div>

<div id="blog-container" class="blog-grid">
  <div class="loading">⏳ Загрузка статей...</div>
</div>

<script>
const GITHUB_USER = "imdbcooper";
const GITHUB_REPO = "som";
const BLOG_FOLDER = "src/site/notes/Published/Blog";
const SITE_URL = "https://smirnov.vercel.app";

let allPosts = [];
let selectedTag = null;

async function loadBlogPosts() {
  try {
    const url = `https://api.github.com/repos/${GITHUB_USER}/${GITHUB_REPO}/contents/${BLOG_FOLDER}`;
    console.log('📥 Загружаю файлы из:', url);
    
    const response = await fetch(url);
    if (!response.ok) throw new Error(`GitHub API ошибка: ${response.status}`);

    const files = await response.json();
    console.log('📂 Все файлы:', files.map(f => f.name));

    const mdFiles = files.filter(f => 
      f && f.name && f.name.endsWith('.md') && 
      f.name !== 'Blog.md' && f.name !== 'Блог.md'
    );

    console.log('✅ Найдено markdown файлов:', mdFiles.length);

    if (mdFiles.length === 0) {
      document.getElementById('blog-container').innerHTML = 
        '<p class="no-posts">Статьи ещё не опубликованы! 📝</p>';
      return;
    }

    const posts = await Promise.all(
      mdFiles.map(file => 
        file.download_url ? loadAndParseFile(file.download_url, file.name) : Promise.resolve(null)
      )
    );

    allPosts = posts
      .filter(p => p && typeof p === 'object' && (p.published === true || p.published === 'true'))
      .sort((a, b) => new Date(b.date) - new Date(a.date));

    console.log('🎉 Валидных статей:', allPosts.length);
    
    if (allPosts.length === 0) {
      document.getElementById('blog-container').innerHTML = '<p class="no-posts">Нет опубликованных статей</p>';
      return;
    }

    renderFilters();
    renderCards(allPosts);
  } catch (error) {
    console.error('❌ Ошибка:', error);
    const container = document.getElementById('blog-container');
    if (container) container.innerHTML = `<p class="no-posts" style="color: red;">⚠️ Ошибка: ${error.message}</p>`;
  }
}

function renderFilters() {
  const filtersContainer = document.getElementById('blog-filters');
  const tags = new Set();
  
  allPosts.forEach(post => {
    if (post.category) tags.add(post.category);
  });

  const tagsArray = Array.from(tags).sort();
  
  const filterButtons = tagsArray.map(tag => 
    `<button class="filter-btn" onclick="filterByTag('${tag}')">${tag}</button>`
  ).join('');

  const clearBtn = `<button class="filter-btn" onclick="filterByTag(null)">Показать все</button>`;
  
  filtersContainer.innerHTML = `<span class="filter-label">Фильтр:</span>${clearBtn}${filterButtons}`;
}

function filterByTag(tag) {
  selectedTag = tag;
  
  document.querySelectorAll('.filter-btn').forEach(btn => {
    btn.classList.remove('active');
  });
  if (tag === null) {
    document.querySelector('.filter-btn').classList.add('active');
  } else {
    document.querySelectorAll('.filter-btn').forEach(btn => {
      if (btn.textContent.trim() === tag) btn.classList.add('active');
    });
  }

  const filtered = tag === null 
    ? allPosts 
    : allPosts.filter(post => post.category === tag);

  renderCards(filtered);
}

async function loadAndParseFile(url, filename) {
  try {
    const response = await fetch(url);
    if (!response.ok) return null;
    const content = await response.text();
    return parseMetadata(content, filename);
  } catch (error) {
    console.error(`❌ Ошибка загрузки ${filename}:`, error);
    return null;
  }
}

function parseMetadata(content, filename) {
  try {
    if (!content || typeof content !== 'string') return null;

    let meta = {};
    let contentStart = 0;

    const yamlMatch = content.match(/^---\s*\n([\s\S]*?)\n---/);
    
    if (yamlMatch && yamlMatch[1]) {
      let blockContent = yamlMatch[1].trim();
      
      if (blockContent.startsWith('{')) {
        console.log('🔍 JSON формат (Digital Garden)');
        try {
          meta = JSON.parse(blockContent);
          console.log('✅ JSON распарсен:', meta);
        } catch (e) {
          console.warn('⚠️ JSON parse error:', e.message);
        }
      } else {
        console.log('🔍 YAML формат');
        const lines = blockContent.split('\n');
        lines.forEach(line => {
          if (!line || !line.trim()) return;
          const colonIdx = line.indexOf(':');
          if (colonIdx === -1) return;
          
          const key = line.substring(0, colonIdx).trim();
          let value = line.substring(colonIdx + 1).trim();
          
          if ((value.startsWith('"') && value.endsWith('"')) || 
              (value.startsWith("'") && value.endsWith("'"))) {
            value = value.slice(1, -1);
          }
          
          if (value === 'true') meta[key] = true;
          else if (value === 'false') meta[key] = false;
          else meta[key] = value;
        });
      }
      contentStart = yamlMatch[0].length;
    }

    if (Object.keys(meta).length === 0) {
      const jsonMatch = content.match(/^\s*\{[\s\S]*?\}\s*\n/);
      if (jsonMatch) {
        try {
          meta = JSON.parse(jsonMatch[0]);
          console.log('✅ JSON в начале распарсен:', meta);
          contentStart = jsonMatch[0].length;
        } catch (e) {
          console.warn('⚠️ JSON parse error:', e.message);
        }
      }
    }

    let published = false;
    if (meta['dg-publish'] === true || meta['dg-publish'] === 'true' ||
        meta['published'] === true || meta['published'] === 'true') {
      published = true;
    }

    const title = String(meta.title || 'Без названия');
    const date = String(meta.date || new Date().toISOString().split('T')[0]);
    let excerpt = meta.excerpt || meta.description || '';
    
    if (!excerpt && contentStart < content.length) {
      excerpt = content.substring(contentStart, contentStart + 150)
        .replace(/[#\-\*]/g, '')
        .replace(/\n/g, ' ')
        .trim() + '...';
    }
    if (!excerpt) excerpt = 'Нет описания';

    let category = 'Статья';
    if (Array.isArray(meta.tags) && meta.tags.length > 0) {
      category = meta.tags[0];
    } else if (typeof meta.tags === 'string') {
      category = meta.tags;
    } else if (meta.category) {
      category = meta.category;
    }

    const emoji = String(meta.emoji || '📝');
    
    // ИСПОЛЬЗУЕМ PERMALINK из фронтматтера!
    let postUrl = '#';
    if (meta.permalink) {
      postUrl = SITE_URL + meta.permalink;
    }

    console.log('✅ Финальный результат:', {title, date, excerpt, category, emoji, published, url: postUrl});

    return {
      title: title,
      date: date,
      excerpt: String(excerpt),
      category: String(category),
      emoji: emoji,
      published: published,
      url: postUrl
    };
  } catch (error) {
    console.error('❌ Ошибка парсинга:', error);
    return null;
  }
}

function renderCards(posts) {
  const container = document.getElementById('blog-container');
  if (!container) return;

  if (!posts || posts.length === 0) {
    container.innerHTML = '<p class="no-posts">По выбранному фильтру нет статей</p>';
    return;
  }

  const html = posts.map((post) => {
    let dateStr = 'Дата неизвестна';
    try {
      if (post.date) {
        const date = new Date(String(post.date));
        if (!isNaN(date.getTime())) {
          dateStr = date.toLocaleDateString('ru-RU', {
            year: 'numeric', 
            month: 'long', 
            day: 'numeric'
          });
        }
      }
    } catch (e) {}

    const title = String(post.title || '').replace(/[<>]/g, '');
    const excerpt = String(post.excerpt || '').replace(/[<>]/g, '').substring(0, 150);
    const category = String(post.category || '').replace(/[<>]/g, '');
    const emoji = String(post.emoji || '📝').substring(0, 2);
    const url = post.url || '#';

    return `
      <a href="${url}" class="blog-card">
        <div class="blog-card-image">${emoji}</div>
        <div class="blog-card-content">
          <h3 class="blog-card-title">${title}</h3>
          <p class="blog-card-excerpt">${excerpt}</p>
          <div class="blog-card-meta">
            <span>${dateStr}</span>
            <span class="blog-card-tag">${category}</span>
          </div>
        </div>
      </a>
    `;
  }).join('');

  container.innerHTML = html;
  console.log('✨ Готово!');
}

console.log('🚀 Начинаем загрузку...');
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', loadBlogPosts);
} else {
  loadBlogPosts();
}
</script>
