---
{"dg-publish":true,"permalink":"/published/blog/","title":"Блог | Мои статьи и туториалы"}
---


# 📝 Блог

Здесь я делюсь знаниями и опытом через статьи, туториалы и размышления.

<script>
  // ✅ ПОЛНОСТЬЮ ЗАЩИЩЁННЫЙ ПАРСЕР
  const GITHUB_USER = "imdbcooper";
  const GITHUB_REPO = "som";
  const BLOG_FOLDER = "src/site/notes/Published/Blog";

  async function loadBlogPosts() {
    try {
      const url = `https://api.github.com/repos/${GITHUB_USER}/${GITHUB_REPO}/contents/${BLOG_FOLDER}`;
      
      console.log('Загружаю файлы из:', url);
      
      const response = await fetch(url);
      
      if (!response.ok) {
        throw new Error(`GitHub API ошибка: ${response.status}`);
      }

      const files = await response.json();
      const mdFiles = files.filter(f => 
        f.name && 
        f.name.endsWith && 
        f.name.endsWith('.md') && 
        f.name !== 'Blog.md' &&
        f.name !== 'Блог.md'
      );

      console.log('Найдено файлов:', mdFiles.length);

      if (mdFiles.length === 0) {
        document.getElementById('blog-container').innerHTML = 
          '<p style="grid-column: 1/-1; text-align: center; color: #999;">Статьи ещё не опубликованы. Создайте первую! 📝</p>';
        return;
      }

      const posts = await Promise.all(
        mdFiles.map(file => {
          if (file.download_url) {
            return loadAndParseFile(file.download_url);
          }
          return Promise.resolve(null);
        })
      );

      const validPosts = posts
        .filter(p => {
          if (!p) return false;
          if (typeof p !== 'object') return false;
          return p.published === true || p.published === 'true';
        })
        .sort((a, b) => {
          try {
            const dateA = new Date(a.date);
            const dateB = new Date(b.date);
            return dateB - dateA;
          } catch (e) {
            return 0;
          }
        });

      console.log('Валидных статей:', validPosts.length);
      console.log('Статьи:', validPosts);
      
      if (validPosts.length === 0) {
        document.getElementById('blog-container').innerHTML = 
          '<p style="grid-column: 1/-1; text-align: center; color: #999;">Нет опубликованных статей</p>';
        return;
      }

      renderCards(validPosts);
    } catch (error) {
      console.error('Ошибка:', error);
      document.getElementById('blog-container').innerHTML = 
        `<p style="grid-column: 1/-1; color: red; text-align: center;">⚠️ Ошибка загрузки: ${error.message}</p>`;
    }
  }

  async function loadAndParseFile(url) {
    try {
      const response = await fetch(url);
      if (!response.ok) {
        console.error(`Ошибка загрузки файла: ${response.status}`);
        return null;
      }
      
      const content = await response.text();
      const parsed = parseMetadata(content);
      console.log('Распарсен файл:', parsed);
      return parsed;
    } catch (error) {
      console.error('Ошибка при загрузке файла:', error);
      return null;
    }
  }

  function parseMetadata(content) {
    try {
      if (!content || typeof content !== 'string') {
        console.warn('Контент не является строкой');
        return null;
      }

      // Извлекаем YAML фронтматтер
      const match = content.match(/^---\s*\n([\s\S]*?)\n---/);
      if (!match || !match) {
        console.warn('Фронтматтер не найден');
        return null;
      }

      const meta = {};
      const yamlContent = match;
      
      // Разбиваем построчно и очищаем
      const lines = yamlContent.split('\n').filter(line => {
        // Убеждаемся что это строка
        if (typeof line !== 'string') return false;
        // Убеждаемся что не пустая после trim
        return line.trim().length > 0;
      });

      console.log('YAML строк:', lines.length);

      lines.forEach((line, index) => {
        try {
          // Очень аккуратно работаем со строкой
          if (!line || typeof line !== 'string') {
            console.warn(`Строка ${index} не валидна:`, line, typeof line);
            return;
          }

          const trimmedLine = String(line).trim();
          if (!trimmedLine) return;

          // Ищем двоеточие
          const colonIndex = trimmedLine.indexOf(':');
          if (colonIndex === -1) {
            console.warn(`Строка ${index} не содержит ':'`, trimmedLine);
            return;
          }

          const key = String(trimmedLine.substring(0, colonIndex)).trim();
          let value = String(trimmedLine.substring(colonIndex + 1)).trim();

          // Удаляем кавычки
          if ((value.startsWith('"') && value.endsWith('"')) || 
              (value.startsWith("'") && value.endsWith("'"))) {
            value = value.slice(1, -1);
          }

          // Конвертируем в нужный тип
          if (value === 'true') {
            meta[key] = true;
          } else if (value === 'false') {
            meta[key] = false;
          } else {
            meta[key] = value;
          }

          console.log(`Ключ: "${key}", значение: "${value}"`);
        } catch (e) {
          console.warn(`Ошибка при парсинге строки ${index}:`, e, line);
        }
      });

      console.log('Распарсено метаданных:', meta);

      // Проверяем статус публикации - очень аккуратно
      let published = false;
      const publishValue = meta['dg-publish'];
      if (publishValue === true || publishValue === 'true') {
        published = true;
      }

      return {
        title: String(meta.title || 'Без названия'),
        date: String(meta.date || new Date().toISOString().split('T')),
        excerpt: String(meta.excerpt || meta.description || 'Нет описания'),
        category: String(meta.category || meta.tags || 'Статья'),
        emoji: String(meta.emoji || '📝'),
        published: published
      };
    } catch (error) {
      console.error('Ошибка при разборе метаданных:', error, content);
      return null;
    }
  }

  function renderCards(posts) {
    const container = document.getElementById('blog-container');
    
    if (!posts || !Array.isArray(posts) || posts.length === 0) {
      container.innerHTML = '<p style="grid-column: 1/-1; text-align: center; color: #999;">Нет статей для отображения</p>';
      return;
    }

    const html = posts.map((post) => {
      // Безопасно форматируем дату
      let dateStr = 'Дата неизвестна';
      try {
        if (post.date) {
          const date = new Date(String(post.date));
          if (!isNaN(date.getTime())) {
            dateStr = date.toLocaleDateString('ru-RU', {
              year: 'numeric',
              month: 'long',
              day: 'numeric'
            });
          }
        }
      } catch (e) {
        console.warn('Ошибка форматирования даты:', post.date, e);
      }

      // Безопасно экранируем HTML
      const title = String(post.title || '').replace(/[<>]/g, '');
      const excerpt = String(post.excerpt || '').replace(/[<>]/g, '');
      const category = String(post.category || '').replace(/[<>]/g, '');
      const emoji = String(post.emoji || '📝').substring(0, 2); // Max 2 символа

      return `
        <div class="blog-card">
          <div class="blog-card-image">${emoji}</div>
          <div class="blog-card-content">
            <h3 class="blog-card-title">${title}</h3>
            <p class="blog-card-excerpt">${excerpt}</p>
            <div class="blog-card-meta">
              <span>${dateStr}</span>
              <span class="blog-card-tag">${category}</span>
            </div>
          </div>
        </div>
      `;
    }).join('');

    container.innerHTML = html;
  }

  // Загружаем статьи при открытии страницы
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', loadBlogPosts);
  } else {
    loadBlogPosts();
  }
</script>
